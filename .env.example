# Cổng chạy Backend Express (Mặc định: 5000)
PORT=5000

# (Tùy chọn) Địa chỉ API Backend nếu triển khai riêng Subdomain (Ví dụ: https://api.toolseo.uk)
# Nếu để trống (mặc định), Frontend sẽ tự động gọi API cùng domain hiện tại (ví dụ: https://toolseo.uk/api/...)
# VITE_API_URL=https://api.toolseo.uk
